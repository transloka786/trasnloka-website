import type {Metadata} from 'next';
import Image from 'next/image';
import Link from 'next/link';
import Reveal from '@/components/Reveal';
import {TEAM,PUBS} from '@/lib/content';
import {PUBLIC_RESEARCH} from '@/lib/publicStory';
export const metadata:Metadata={title:'Our Team',description:'KritRNA’s co-founders, scientific foundation and developing advisory network.'};
const publications=PUBS.map(p=>p.venue==='Nature Communications'?{...p,title:PUBLIC_RESEARCH.title,topic:PUBLIC_RESEARCH.note,role:'Co-first author',doi:PUBLIC_RESEARCH.href,pubmed:'https://pubmed.ncbi.nlm.nih.gov/38580646/'}:p);
export default function Team(){return <div className="noir-about"><section className="page-hero"><div className="wrap"><Reveal><div className="eyebrow">THE PEOPLE BEHIND THE PURPOSE</div><h1 className="h1">Scientific leadership.<br/><em>Shared founding responsibility.</em></h1><p className="lead">KritRNA is led by two equal co-founders and directors. Dr. Nikhil Bharti leads scientific and technology development. Pragati Bhaisora leads outreach, awareness, stakeholder communication and community engagement.</p><Link className="underline-link" href="/about#founding-story">Read the founding story ↗</Link></Reveal></div></section><section><div className="wrap"><div className="principle-spread">{[TEAM.lead,TEAM.outreach].map(person=><Reveal key={person.name}><article style={{paddingTop:0}}><div className="story-portrait"><Image src={person.photo} alt={person.name} width={500} height={620} sizes="(max-width: 640px) 88vw, 420px"/></div><h2 className="h2" style={{marginTop:24}}>{person.name}</h2><p className="micro" style={{marginTop:12,color:'var(--gold)'}}>{person.role}</p><p style={{marginTop:18}}>{person.bio}</p></article></Reveal>)}</div></div></section><section><div className="wrap"><div className="sec-head"><div className="eyebrow">EXTENDED NETWORK</div><h2 className="h2">Building capability around the founders.</h2><p>The developing build and advisory network is shown with its current recorded status. Pending discussions are not confirmed appointments or institutional partnerships.</p></div><div className="standard-grid"><Reveal><article><span className="micro">AI / ML & COMPUTATIONAL BIOLOGY</span>{TEAM.build.map(person=><div className="public-question-list" key={person.name}><h3>{person.name}</h3><p>{person.note}</p></div>)}</article></Reveal><Reveal><article><span className="micro">SCIENTIFIC & CLINICAL ADVISORY</span>{TEAM.advisory.map(person=><div className="public-question-list" key={person.name}><h3>{person.name}</h3><p>{person.note}</p></div>)}</article></Reveal></div></div></section><section><div className="wrap"><div className="sec-head"><div className="eyebrow">THE ACADEMIC FOUNDATION</div><h2 className="h2">Founder research record.</h2><p>Published research is part of our scientific foundation. It is distinct from the developing company platform and from evidence generated for a KritRNA candidate.</p></div><div className="public-question-list">{publications.map(publication=><Reveal key={publication.venue}><article><div className="micro">{publication.venue} · {publication.year} · {publication.role}</div><h3 style={{marginTop:15}}>{publication.title}</h3><p>{publication.topic}</p><div style={{display:'flex',gap:30,flexWrap:'wrap'}}>{publication.doi&&<a className="underline-link" href={publication.doi} target="_blank" rel="noreferrer">Read the paper ↗</a>}<a className="underline-link" href={publication.pubmed} target="_blank" rel="noreferrer">{publication.doi?'PubMed ↗':'Search PubMed ↗'}</a></div></article></Reveal>)}</div><p className="scene-note" style={{marginTop:25}}>Former research institutions are not represented as current company affiliates or endorsers. A publication record does not establish company ownership of university intellectual property.</p></div></section></div>;}
